{% extends 'base.html.twig' %}

{% block title %}BlogCollection{% endblock %}

{% block body %}
    <h1>BlogCollection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blog_collection.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ blog_collection.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_blog_collection_index') }}">back to list</a>

    <a href="{{ path('app_blog_collection_edit', {'id': blog_collection.id}) }}">edit</a>

    {{ include('blog_collection/_delete_form.html.twig') }}
{% endblock %}
