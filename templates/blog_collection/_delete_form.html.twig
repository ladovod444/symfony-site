<form method="post" action="{{ path('app_blog_collection_delete', {'id': blog_collection.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog_collection.id) }}">
    <button class="btn">Delete</button>
</form>
